# Postgres image only (user / password / database name).
# The API does not read this file; local Rider uses appsettings.Development.json,
# and the Compose app service gets ConnectionStrings__DefaultConnection.
POSTGRES_USER=verticalslices
POSTGRES_PASSWORD=verticalslices
POSTGRES_DB=verticalslices
